/*
NOTE: We don't create admin user here. We populate mock-up data here for development.

MongoDB automatically create a admin user base on these environment variables:

- MONGO_INITDB_ROOT_USERNAME
- MONGO_INITDB_ROOT_PASSWORD

You should set these environment variables in <root-directory>/backend/.env_file.
You can check this template file: <root-directory>/backend/.env_file_template.
*/
